{ "name": "Notes / footer", "description": "Auto-updates with the model name each print; supports a manual text override (via the /api/note endpoint).", "recommendedSize": {"w":600,"h":40}, "tags":["info"] }
